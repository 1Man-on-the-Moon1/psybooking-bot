# 🚀 Руководство по переносу бота на хостинг

Подробная инструкция по развертыванию Telegram бота на вашем сервере или VPS.

---

## 📋 Содержание

1. [Требования к серверу](#требования-к-серверу)
2. [Варианты хостинга](#варианты-хостинга)
3. [Пошаговая инструкция](#пошаговая-инструкция)
4. [Настройка автозапуска](#настройка-автозапуска)
5. [Мониторинг и логи](#мониторинг-и-логи)
6. [Обновление бота](#обновление-бота)
7. [Решение проблем](#решение-проблем)

---

## 🖥 Требования к серверу

### Минимальные требования:
- **ОС:** Ubuntu 20.04+ / Debian 10+ / CentOS 7+
- **RAM:** 512 MB (рекомендуется 1 GB)
- **Диск:** 5 GB свободного места
- **CPU:** 1 ядро
- **Интернет:** Постоянное подключение

### Необходимое ПО:
- Python 3.8+
- pip3
- SQLite3 (обычно предустановлен)
- systemd (для автозапуска)

---

## 🌐 Варианты хостинга

### 1. **VPS/VDS серверы** (Рекомендуется)

**Преимущества:**
- ✅ Полный контроль
- ✅ Постоянная работа 24/7
- ✅ Можно запустить несколько ботов
- ✅ Доступ по SSH

**Популярные провайдеры:**

#### 🇷🇺 Российские:
- **Timeweb** - от 199₽/мес
  - https://timeweb.com/
  - Простая панель управления
  - Хорошая поддержка

- **Beget** - от 150₽/мес
  - https://beget.com/
  - Надежный хостинг
  - Быстрая настройка

- **Selectel** - от 250₽/мес
  - https://selectel.ru/
  - Профессиональный уровень
  - Гибкие тарифы

- **REG.RU** - от 190₽/мес
  - https://www.reg.ru/
  - Известный провайдер
  - Круглосуточная поддержка

#### 🌍 Международные:
- **DigitalOcean** - от $6/мес
  - https://www.digitalocean.com/
  - Простой интерфейс
  - Много документации

- **Vultr** - от $5/мес
  - https://www.vultr.com/
  - Быстрые серверы
  - Много локаций

- **Hetzner** - от €4.5/мес
  - https://www.hetzner.com/
  - Отличная цена/качество
  - Европейские дата-центры

- **Linode** - от $5/мес
  - https://www.linode.com/
  - Стабильная работа
  - Хорошая поддержка

### 2. **PaaS платформы**

#### Railway
- https://railway.app/
- Бесплатный тариф: $5 кредитов/мес
- Автоматический деплой из GitHub
- Простая настройка

#### Render
- https://render.com/
- Бесплатный тариф (с ограничениями)
- Автоматическое развертывание
- SSL из коробки

#### Heroku
- https://www.heroku.com/
- Платные тарифы от $7/мес
- Простой деплой
- Много интеграций

### 3. **Домашний сервер**

**Если у вас есть:**
- Старый компьютер/ноутбук
- Raspberry Pi
- Постоянный интернет

**Преимущества:**
- ✅ Бесплатно
- ✅ Полный контроль

**Недостатки:**
- ❌ Нужен постоянный интернет
- ❌ Расходы на электричество
- ❌ Нет профессиональной поддержки

---

## 📝 Пошаговая инструкция

### Шаг 1: Подключение к серверу

#### Через SSH (Linux/Mac):
```bash
ssh username@your-server-ip
```

#### Через PuTTY (Windows):
1. Скачайте PuTTY: https://www.putty.org/
2. Введите IP сервера
3. Нажмите "Open"
4. Введите логин и пароль

---

### Шаг 2: Обновление системы

```bash
# Обновить список пакетов
sudo apt update

# Обновить установленные пакеты
sudo apt upgrade -y
```

---

### Шаг 3: Установка Python и зависимостей

```bash
# Установить Python 3 и pip
sudo apt install python3 python3-pip python3-venv -y

# Проверить версию Python
python3 --version
# Должно быть 3.8 или выше
```

---

### Шаг 4: Создание пользователя для бота (рекомендуется)

```bash
# Создать пользователя
sudo adduser botuser

# Добавить в группу sudo (опционально)
sudo usermod -aG sudo botuser

# Переключиться на пользователя
sudo su - botuser
```

---

### Шаг 5: Загрузка файлов бота

#### Вариант A: Через SCP (с вашего компьютера)

```bash
# Скопировать архив на сервер
scp psybooking-bot.tar.gz username@your-server-ip:/home/username/

# На сервере распаковать
cd /home/username
tar -xzf psybooking-bot.tar.gz
```

#### Вариант B: Через Git (если код в репозитории)

```bash
# Установить git
sudo apt install git -y

# Клонировать репозиторий
git clone https://github.com/your-username/psybooking-bot.git
cd psybooking-bot
```

#### Вариант C: Через wget (если файл доступен по URL)

```bash
# Скачать архив
wget https://your-url.com/psybooking-bot.tar.gz

# Распаковать
tar -xzf psybooking-bot.tar.gz
cd psybooking-bot
```

---

### Шаг 6: Создание виртуального окружения

```bash
# Перейти в папку проекта
cd /home/username/psybooking-bot

# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate

# В приглашении должно появиться (venv)
```

---

### Шаг 7: Установка зависимостей

```bash
# Установить все пакеты
pip install -r requirements.txt

# Проверить установку
pip list
```

---

### Шаг 8: Настройка конфигурации

```bash
# Скопировать пример .env
cp .env.example .env

# Отредактировать .env
nano .env
```

**Заполните файл .env:**

```bash
# Telegram Bot Token (от @BotFather)
TELEGRAM_BOT_TOKEN=8212656806:AAFnXi_ySVUVLN3qS7CeuDEM9KDz0aNdwRY

# Ваш Telegram ID (для администрирования)
ADMIN_TELEGRAM_ID=783321437

# Google Calendar настройки
GOOGLE_CREDENTIALS_FILE=credentials.json
GOOGLE_TOKEN_FILE=data/token.pickle
GOOGLE_CALENDAR_ID=primary

# Часовой пояс
PRIMARY_TZ=Europe/Minsk

# База данных
DATABASE_PATH=data/psybooking.db
```

**Сохранить:** `Ctrl+O`, `Enter`, `Ctrl+X`

---

### Шаг 9: Загрузка credentials.json

```bash
# Создать папку data
mkdir -p data

# Загрузить credentials.json на сервер
# Вариант A: через SCP
scp credentials.json username@your-server-ip:/home/username/psybooking-bot/

# Вариант B: через nano (скопировать содержимое)
nano credentials.json
# Вставить содержимое, сохранить
```

---

### Шаг 10: Инициализация базы данных

```bash
# Инициализировать БД
python3 manage.py init

# Настроить рабочие часы
python3 manage.py working-hours set 1 09:00 18:00  # Понедельник
python3 manage.py working-hours set 2 09:00 18:00  # Вторник
python3 manage.py working-hours set 3 09:00 18:00  # Среда
python3 manage.py working-hours set 4 09:00 18:00  # Четверг
python3 manage.py working-hours set 5 09:00 18:00  # Пятница

# Проверить настройки
python3 manage.py working-hours show
```

---

### Шаг 11: Авторизация Google Calendar

```bash
# Запустить авторизацию
python3 auth_google.py

# Скопировать URL из вывода
# Открыть в браузере
# Разрешить доступ
# Скопировать код авторизации
# Вставить в терминал

# Должно появиться: ✅ Авторизация успешна!
```

---

### Шаг 12: Тестовый запуск

```bash
# Запустить бота
python3 bot.py

# Должно появиться:
# INFO - Бот запущен...

# Проверить в Telegram - отправить /start

# Остановить: Ctrl+C
```

---

## 🔄 Настройка автозапуска

### Вариант 1: systemd (Рекомендуется)

#### Создать systemd service:

```bash
# Создать файл сервиса
sudo nano /etc/systemd/system/psybooking-bot.service
```

**Содержимое файла:**

```ini
[Unit]
Description=PsyBooking Telegram Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/psybooking-bot
Environment="PATH=/home/botuser/psybooking-bot/venv/bin"
ExecStart=/home/botuser/psybooking-bot/venv/bin/python3 /home/botuser/psybooking-bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Замените:**
- `botuser` → ваш пользователь
- `/home/botuser/psybooking-bot` → путь к боту

**Сохранить:** `Ctrl+O`, `Enter`, `Ctrl+X`

#### Запустить сервис:

```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable psybooking-bot

# Запустить бота
sudo systemctl start psybooking-bot

# Проверить статус
sudo systemctl status psybooking-bot

# Должно быть: Active: active (running)
```

#### Управление сервисом:

```bash
# Запустить
sudo systemctl start psybooking-bot

# Остановить
sudo systemctl stop psybooking-bot

# Перезапустить
sudo systemctl restart psybooking-bot

# Проверить статус
sudo systemctl status psybooking-bot

# Посмотреть логи
sudo journalctl -u psybooking-bot -f
```

---

### Вариант 2: screen (Простой способ)

```bash
# Установить screen
sudo apt install screen -y

# Создать сессию
screen -S psybot

# Активировать venv
cd /home/username/psybooking-bot
source venv/bin/activate

# Запустить бота
python3 bot.py

# Отключиться от сессии: Ctrl+A, затем D

# Вернуться к сессии
screen -r psybot

# Список сессий
screen -ls
```

---

### Вариант 3: tmux (Альтернатива screen)

```bash
# Установить tmux
sudo apt install tmux -y

# Создать сессию
tmux new -s psybot

# Запустить бота
cd /home/username/psybooking-bot
source venv/bin/activate
python3 bot.py

# Отключиться: Ctrl+B, затем D

# Вернуться
tmux attach -t psybot

# Список сессий
tmux ls
```

---

### Вариант 4: nohup (Базовый способ)

```bash
# Запустить в фоне
cd /home/username/psybooking-bot
source venv/bin/activate
nohup python3 bot.py > bot.log 2>&1 &

# Сохранить PID
echo $! > bot.pid

# Остановить
kill $(cat bot.pid)

# Посмотреть логи
tail -f bot.log
```

---

## 📊 Мониторинг и логи

### Просмотр логов systemd:

```bash
# Последние 100 строк
sudo journalctl -u psybooking-bot -n 100

# Следить за логами в реальном времени
sudo journalctl -u psybooking-bot -f

# Логи за сегодня
sudo journalctl -u psybooking-bot --since today

# Логи за последний час
sudo journalctl -u psybooking-bot --since "1 hour ago"
```

### Просмотр файловых логов:

```bash
# Последние 50 строк
tail -50 /home/username/psybooking-bot/bot.log

# Следить в реальном времени
tail -f /home/username/psybooking-bot/bot.log

# Поиск ошибок
grep ERROR /home/username/psybooking-bot/bot.log
```

### Проверка статуса:

```bash
# Статус сервиса
sudo systemctl status psybooking-bot

# Проверка процесса
ps aux | grep bot.py

# Использование ресурсов
top -p $(pgrep -f bot.py)
```

---

## 🔧 Обновление бота

### Способ 1: Через Git

```bash
# Остановить бота
sudo systemctl stop psybooking-bot

# Обновить код
cd /home/username/psybooking-bot
git pull origin main

# Обновить зависимости
source venv/bin/activate
pip install -r requirements.txt --upgrade

# Запустить бота
sudo systemctl start psybooking-bot

# Проверить статус
sudo systemctl status psybooking-bot
```

### Способ 2: Ручное обновление

```bash
# Остановить бота
sudo systemctl stop psybooking-bot

# Сделать бэкап
cd /home/username
cp -r psybooking-bot psybooking-bot-backup

# Загрузить новые файлы
# (через SCP или wget)

# Запустить бота
sudo systemctl start psybooking-bot
```

---

## 🔒 Безопасность

### Настройка firewall:

```bash
# Установить ufw
sudo apt install ufw -y

# Разрешить SSH
sudo ufw allow 22/tcp

# Включить firewall
sudo ufw enable

# Проверить статус
sudo ufw status
```

### Защита файлов:

```bash
# Права на .env
chmod 600 .env

# Права на credentials.json
chmod 600 credentials.json

# Права на папку data
chmod 700 data
```

### Регулярные обновления:

```bash
# Обновлять систему раз в неделю
sudo apt update && sudo apt upgrade -y

# Настроить автоматические обновления
sudo apt install unattended-upgrades -y
sudo dpkg-reconfigure -plow unattended-upgrades
```

---

## 🐛 Решение проблем

### Бот не запускается

**Проверить логи:**
```bash
sudo journalctl -u psybooking-bot -n 50
```

**Частые причины:**
1. Неправильный путь в systemd service
2. Не установлены зависимости
3. Неправильный TELEGRAM_BOT_TOKEN
4. Нет прав на файлы

### Ошибка "ModuleNotFoundError"

```bash
# Активировать venv
source venv/bin/activate

# Переустановить зависимости
pip install -r requirements.txt --force-reinstall
```

### Ошибка Google Calendar

```bash
# Удалить старый токен
rm data/token.pickle

# Авторизоваться заново
python3 auth_google.py
```

### Бот отвечает медленно

**Проверить ресурсы:**
```bash
# Использование CPU и RAM
htop

# Использование диска
df -h

# Сетевая задержка
ping api.telegram.org
```

### База данных повреждена

```bash
# Сделать бэкап
cp data/psybooking.db data/psybooking.db.backup

# Проверить целостность
sqlite3 data/psybooking.db "PRAGMA integrity_check;"

# Если повреждена - восстановить из бэкапа
# или пересоздать
python3 manage.py init
```

---

## 📦 Бэкап

### Автоматический бэкап (cron):

```bash
# Создать скрипт бэкапа
nano /home/username/backup.sh
```

**Содержимое:**
```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/home/username/backups"
mkdir -p $BACKUP_DIR

# Бэкап базы данных
cp /home/username/psybooking-bot/data/psybooking.db \
   $BACKUP_DIR/psybooking_$DATE.db

# Удалить старые бэкапы (старше 7 дней)
find $BACKUP_DIR -name "psybooking_*.db" -mtime +7 -delete
```

**Сделать исполняемым:**
```bash
chmod +x /home/username/backup.sh
```

**Добавить в cron:**
```bash
# Открыть crontab
crontab -e

# Добавить строку (бэкап каждый день в 3:00)
0 3 * * * /home/username/backup.sh
```

---

## 📞 Поддержка

### Полезные команды:

```bash
# Проверить версию Python
python3 --version

# Проверить установленные пакеты
pip list

# Проверить использование портов
sudo netstat -tulpn | grep python

# Проверить системные логи
sudo tail -f /var/log/syslog
```

### Документация:

- Python Telegram Bot: https://python-telegram-bot.org/
- Google Calendar API: https://developers.google.com/calendar
- systemd: https://www.freedesktop.org/software/systemd/man/

---

## ✅ Чеклист развертывания

- [ ] Сервер выбран и настроен
- [ ] Python 3.8+ установлен
- [ ] Код бота загружен на сервер
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] Файл .env настроен
- [ ] credentials.json загружен
- [ ] Google Calendar авторизован
- [ ] База данных инициализирована
- [ ] Рабочие часы настроены
- [ ] Тестовый запуск успешен
- [ ] systemd service настроен
- [ ] Автозапуск включен
- [ ] Бот работает 24/7
- [ ] Логи проверены
- [ ] Бэкап настроен
- [ ] Firewall настроен
- [ ] Бот протестирован в Telegram

---

**Готово! Ваш бот работает на постоянном хостинге! 🎉**

*Обновлено: 11 декабря 2024*
