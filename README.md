# PsyBooking Telegram Bot

Telegram бот для записи на консультации к психологу с интеграцией Google Calendar.

## Возможности

### Для клиентов:
- 📅 Просмотр доступных дат и времени
- ✅ Запись на консультацию через интерактивный интерфейс
- 🔗 Автоматическое получение ссылки на событие в календаре
- 📋 Просмотр своих записей
- 🔔 Напоминания о консультациях (через Google Calendar)

### Для психолога:
- 🗓 Автоматическое создание событий в Google Calendar
- 🚫 Защита от двойного бронирования
- ⚙️ Настраиваемое расписание работы
- 📊 Хранение информации о записях в базе данных

### Технические возможности:
- ⏰ Корректная работа с часовым поясом (Europe/Minsk)
- 🛡 Rate limiting (защита от спама)
- 🔒 Ограничение количества активных записей на пользователя
- 📝 Полное логирование всех операций

## Архитектура

Система состоит из следующих компонентов:

```
psybooking-bot/
├── bot.py                 # Главный файл бота
├── database.py            # Работа с SQLite БД
├── google_calendar.py     # Интеграция с Google Calendar API
├── scheduler.py           # Расчет свободных слотов
├── config.py              # Конфигурация
├── requirements.txt       # Python зависимости
├── .env.example           # Пример переменных окружения
├── README.md              # Документация
└── data/
    ├── psybooking.db      # База данных
    └── token.pickle       # Google OAuth токен
```

## Установка

### 1. Требования

- Python 3.11+
- Telegram Bot Token (от @BotFather)
- Google Cloud Project с включенным Calendar API

### 2. Клонирование и установка зависимостей

```bash
cd psybooking-bot
pip3 install -r requirements.txt
```

### 3. Настройка Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Следуйте инструкциям и получите токен
4. Настройте команды бота:

```
/setcommands

start - Начать работу с ботом
book - Записаться на консультацию
slots - Посмотреть доступные слоты
mybookings - Мои записи
help - Справка
```

### 4. Настройка Google Calendar API

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google Calendar API:
   - APIs & Services → Library
   - Найдите "Google Calendar API"
   - Нажмите "Enable"
4. Создайте OAuth 2.0 credentials:
   - APIs & Services → Credentials
   - Create Credentials → OAuth client ID
   - Application type: Desktop app
   - Скачайте JSON файл
5. Сохраните файл как `credentials.json` в корне проекта

### 5. Настройка переменных окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
GOOGLE_CALENDAR_ID=primary
ADMIN_TELEGRAM_IDS=ваш_telegram_id
```

### 6. Первый запуск

При первом запуске бот попросит авторизоваться в Google:

```bash
python3 bot.py
```

1. Откроется браузер с запросом на авторизацию
2. Войдите в Google аккаунт психолога
3. Разрешите доступ к календарю
4. Токен будет сохранен в `data/token.pickle`

## Использование

### Запуск бота

```bash
python3 bot.py
```

### Команды бота

- `/start` - Приветствие и главное меню
- `/book` - Начать процесс записи
- `/slots` - Показать ближайшие 10 доступных слотов
- `/mybookings` - Показать активные записи пользователя
- `/help` - Справка по использованию

### Процесс записи

1. Пользователь нажимает "Записаться" или отправляет `/book`
2. Бот показывает доступные даты (кнопки "Сегодня", "Завтра" + список дат)
3. После выбора даты показываются свободные временные слоты
4. После выбора слота:
   - Создается запись в БД
   - Создается событие в Google Calendar
   - Пользователь получает подтверждение со ссылкой

## Конфигурация

### Основные настройки (config.py)

```python
PRIMARY_TZ = 'Europe/Minsk'                    # Часовой пояс
MIN_HOURS_BEFORE_BOOKING = 3                   # Минимум часов до записи
SESSION_DURATION_MINUTES = 60                  # Длительность сессии
MAX_ACTIVE_BOOKINGS_PER_USER = 3               # Макс. активных записей
RATE_LIMIT_REQUESTS_PER_MINUTE = 10            # Rate limit
DAYS_AHEAD_TO_SHOW = 14                        # Дней вперед для показа
```

### Настройка рабочих часов

Рабочие часы настраиваются в базе данных в таблице `working_hours`.

По умолчанию установлено:
- Понедельник - Пятница: 10:00 - 19:00
- Суббота, Воскресенье: выключены

Для изменения можно использовать SQL:

```sql
-- Изменить рабочие часы для понедельника (день 1)
UPDATE working_hours 
SET start_time = '09:00', end_time = '18:00' 
WHERE day_of_week = 1;

-- Включить субботу (день 6)
UPDATE working_hours 
SET is_active = 1 
WHERE day_of_week = 6;
```

Дни недели: 0 = Воскресенье, 1 = Понедельник, ..., 6 = Суббота

## База данных

### Структура таблиц

#### settings
Хранит настройки системы (timezone, минимальное время до записи и т.д.)

#### working_hours
Рабочие часы по дням недели

#### bookings
Все записи клиентов с информацией о статусе и связи с Google Calendar

#### rate_limits
Временные данные для контроля частоты запросов

### Резервное копирование

Рекомендуется регулярно создавать резервные копии базы данных:

```bash
cp data/psybooking.db data/psybooking.db.backup.$(date +%Y%m%d)
```

## Безопасность

### Защита данных

- ✅ Токены хранятся в переменных окружения
- ✅ Google OAuth токен в отдельном файле
- ✅ Rate limiting для защиты от спама
- ✅ Ограничение количества активных записей

### Рекомендации

1. Не коммитьте `.env` и `credentials.json` в git
2. Регулярно обновляйте зависимости
3. Используйте webhook вместо polling в продакшене
4. Настройте мониторинг и логирование

## Troubleshooting

### Бот не отвечает

1. Проверьте токен в `.env`
2. Убедитесь, что бот запущен
3. Проверьте логи на наличие ошибок

### Ошибки Google Calendar

1. Проверьте, что Calendar API включен в Google Cloud Console
2. Убедитесь, что `credentials.json` корректен
3. Удалите `data/token.pickle` и пройдите авторизацию заново

### Слоты не отображаются

1. Проверьте настройки рабочих часов в БД
2. Убедитесь, что `MIN_HOURS_BEFORE_BOOKING` не слишком большой
3. Проверьте, что в Google Calendar нет конфликтующих событий

### Двойное бронирование

Система защищена от двойного бронирования через:
- Уникальный индекс в БД на `start_time_utc`
- Проверку занятости перед созданием записи
- Транзакционность операций

Если все же произошло - проверьте логи и синхронизацию с Google Calendar.

## Развитие проекта

### Планируемые улучшения

- [ ] Веб-панель для психолога
- [ ] Возможность отмены записи через бота
- [ ] Напоминания за день до консультации
- [ ] Выбор длительности сессии (30/60/90 минут)
- [ ] Поддержка нескольких психологов
- [ ] Интеграция с платежными системами
- [ ] Мультиязычность

### Вклад в проект

Если вы хотите улучшить проект:
1. Создайте fork
2. Внесите изменения
3. Отправьте pull request

## Лицензия

MIT License

## Поддержка

При возникновении проблем:
1. Проверьте раздел Troubleshooting
2. Изучите логи бота
3. Создайте issue с описанием проблемы

---

**Автор**: PsyBooking Bot
**Версия**: 1.0.0
**Дата**: 2024
